# MySQL Configuration
MYSQL_ROOT_PASSWORD=changeme_root_password
MYSQL_DATABASE=drupal
MYSQL_USER=drupal
MYSQL_PASSWORD=changeme_drupal_password

# Domain Configuration
DOMAIN_SUFFIX=example.com
LETSENCRYPT_EMAIL=admin@example.com

# PHP Configuration
PHP_VERSION=8.3
XDEBUG_MODE=off

# Cloudflare API credentials for SSL (optional - only needed for wildcard SSL)
# Get these from: https://dash.cloudflare.com/profile/api-tokens
# Create token with "Edit zone DNS" permissions
CF_Token=your_cloudflare_api_token_here
CF_Account_ID=your_cloudflare_account_id_here
CF_Zone_ID=your_cloudflare_zone_id_here
